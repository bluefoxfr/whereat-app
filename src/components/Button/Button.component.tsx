import React, {FunctionComponent} from "react";

type ButtonProps = {
  isReset: boolean,
};

export const Logo: FunctionComponent<ButtonProps> = ({
  isReset,
  ...props
}) => {
  return(
    '';
  )
};
